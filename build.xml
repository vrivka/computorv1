<?xml version="1.0"?>
<project name="computerv1" default="run">
    <property name="home_path" location="/Users/vrivka/computorv1"/>
    <property name="src" location="${home_path}/src"/>
    <property name="build" location="${home_path}/build"/>
    <property name="classes" location="${build}/classes/"/>
    <target name="compile">
        <mkdir dir="${classes}"/>
        <javac srcdir="${src}" destdir="${classes}" includeAntRuntime="false"/>
    </target>
    <target name="package" depends="compile">
        <jar destfile="${build}/${ant.project.name}.jar" basedir="${classes}">
            <manifest>
                <attribute name="Main-Class" value="com.app.Main"/>
            </manifest>
        </jar>
    </target>
    <target name="run" depends="package">
        <java jar="${build}/${ant.project.name}.jar" fork="true"/>
    </target>
    <target name="clean">
        <delete dir="${build}"/>
    </target>
</project>